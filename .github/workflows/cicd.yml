name: Test

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - run: |
          output=$(python test.py)
          echo "::set-output name=test_output:$output"
        id: step1

      - run: |
          echo ${{ steps.check.outputs.test_output }}
